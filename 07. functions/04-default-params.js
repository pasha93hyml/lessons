// let userName = "Tor";

// function greetUser(user = "Guest") {
//     console.log(`Hello ${user}`);
// }

// greetUser(userName);
// greetUser("Sara");

// greetUser();

// function userData(name = "Guest", age = 18, status, orders) {
//     console.log(
//         `Hello ${name}, you are ${age}. Status ${status}. Orders ${orders}`
//     );
// }

// userData("Vova", 25, "Good byer", 150);
