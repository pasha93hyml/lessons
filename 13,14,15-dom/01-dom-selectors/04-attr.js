// h1Ref
// ulRef
// formRef

// let yahooRef = document.querySelectorAll("a")[1];
// console.log(yahooRef);

// =======================================
// Set Attribute
// yahooRef.setAttribute("target", "_blanc");

// let imgRef = document.querySelectorAll("img");
// console.log(imgRef);
// imgRef.forEach((image) => {
//     image.setAttribute("alt", "pizza");
//     image.setAttribute("width", 150);
//     image.classList.add("main-image");
// });

// let pizzaRef = document.querySelectorAll("img")[0];
// console.log(pizzaRef);

// pizzaRef.setAttribute("alt", "pizza");
// pizzaRef.setAttribute("width", 150);
// pizzaRef.classList.add("main-image");

// pizzaRef.width = 300;
// pizzaRef.alt = "pizza";

// ======================================
// Get Attribute
// let yahooRef = document.querySelectorAll("a")[1];
// console.log(yahooRef.getAttribute("href"));

let pizzaRef = document.querySelectorAll("img")[0];
// console.log(pizzaRef.getAttribute("src"));

// ====================================
// Remove Attribute
// pizzaRef.removeAttribute("src");
// pizzaRef.src = "";

// ===========================================================
// Short syntax for a and img
// pizzaRef.src = "";
// pizzaRef.target  = "_blanc";
